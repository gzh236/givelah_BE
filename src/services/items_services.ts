import { Request, Response } from "express";
import Item from "../db/models/item";

module.exports = {
  // create item
  createItemService: (req: Request, res: Response) => {
    // validate form input
  },
};
